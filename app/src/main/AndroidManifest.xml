<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
        
    <!-- AndroidManifest.xml -->
<application
        android:name="com.rk.terminal.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="false"
        android:theme="@style/Theme.Karbon"
        tools:targetApi="tiramisu">
    
    <meta-data android:name="wakelock" android:value="0"/>
    
    <provider
            android:name=".AlpineDocumentProvider"
            android:authorities="${applicationId}.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:icon="@mipmap/ic_launcher"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER" />
            </intent-filter>
    </provider>
    
    <activity android:name="com.meinname.loginapp3.MyCustomActivity"
                  android:label="@string/app_name"
                  android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|fontScale|uiMode|uiMode|screenSize|smallestScreenSize|layoutDirection|density"
                  android:screenOrientation="portrait"
                  android:exported="true"
                  android:theme="@style/KivySupportCutout"
                  
                  android:launchMode="singleTask">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter><!-- ToDo: Need more meta-data. Adapt accordingly -->
            <!-- https://doc.qt.io/qt-6/android-manifest-file-configuration.html#qt-specific-meta-data -->
            <meta-data
                    android:name="android.app.lib_name"
                    android:value="main"/>

            <meta-data
                    android:name="android.app.extract_android_style"
                    android:value="minimal" />
    </activity>
    
    
    <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="loginapp" />
    

    <!-- ★ PYTHON ACTIVITY (für Qt) ★ -->
    <activity
        android:name="org.kivy.android.PythonActivity"
        android:configChanges="orientation|screenSize|keyboardHidden"
        android:exported="false"
        android:label="Python" />

    <!-- ★ TERMINAL ACTIVITY (für Core) ★ -->
    <activity
        android:name="com.rk.terminal.ui.activities.terminal.MainActivity"
        android:exported="false"
        android:label="Terminal"
        android:windowSoftInputMode="adjustResize" />

    <!-- ★ SERVICE ★ -->
    <service
        android:name="com.meinname.loginapp3.ServiceMyservice"
        android:process=":service_myservice" />
        
    <service android:name="com.rk.terminal.service.SessionService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

                <!--
                 android:extractNativeLibs="true" = needed for smaller apk size
                 android:requestLegacyExternalStorage="true"
                 android:allowNativeHeapPointerTagging="false"
                -->
    </application>

</manifest>